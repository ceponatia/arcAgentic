// Common utility types and helpers for characters domain.
// Re-export Result type from @arcagentic/utils for backward compatibility
export { type Result, ok, err } from '@arcagentic/utils';

export { resolveDataDir } from './dataDir.js';
